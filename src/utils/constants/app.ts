/**
 * Application Constants
 */
const APP = Object.freeze({
  DISPLAY_NAME: 'My App',
  VERSION: '0.1.0',

  NAMES: Object.freeze({
    MAIN: Object.freeze({
      V0: '[API] Main V0',
    }),
  }),

  LOCAL_STORAGE: Object.freeze({
    PREFIX: '@App',
  }),
})

export { APP }
