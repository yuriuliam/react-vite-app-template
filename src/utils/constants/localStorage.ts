const LOCAL_STORAGE = Object.freeze({
  AUTH: Object.freeze({
    TOKEN: 'auth:token',
    USER: 'auth:user',
  }),

  THEME: Object.freeze({
    DARK: 'theme:dark',
  }),
})

export { LOCAL_STORAGE }
