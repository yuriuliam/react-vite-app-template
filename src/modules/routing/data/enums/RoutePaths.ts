/**
 * All App Routes, respecting Router's `RoutePathLike`
 *
 * @see {App.Domain.Routing.RoutePathLike}
 */
enum RoutePaths {
  Home = '/',
  SignIn = '/sign-in',
  SignOut = '/sign-out',
}

export { RoutePaths }
